using System;

namespace delegateTutorial
{
    public enum ActionType
    {
        None = 0,
        Add = 1,
        Remove = 2,
        SendDirection = 4,
        SendAll = 8,
        List = 16
    }
}